import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const pkg = JSON.parse(await fs.readFile(path.join(root, "package.json"), "utf8"));
const buildDate = new Date().toISOString().slice(0, 10);

const content = `/*
 * PaperDistill
 * Copyright (c) 2026 Petr Nazarov, Luxembourg Institute of Health (LIH)
 *
 * Released under the MIT License.
 * Developed with significant assistance from Anthropic Claude Code.
 * Responsibility for any bugs remains under active investigation.
 *
 * See LICENSE for details.
 */

// Auto-generated by scripts/generate-build-info.mjs. Do not edit by hand.
export const APP_VERSION = "${pkg.version}";
export const BUILD_DATE = "${buildDate}";
`;

await fs.writeFile(path.join(root, "src/main/buildInfo.ts"), content, "utf8");
console.log(`Wrote src/main/buildInfo.ts (version ${pkg.version}, build date ${buildDate})`);
